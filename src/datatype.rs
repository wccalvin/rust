pub fn run() {
    // integer
    // by default i32 is used
    let x: u8 = 255;
    println!("value of x is {}", x);

    // float
    // by default f64 is used (double precision)
    let y = -3.412;
    println!("value of y is {}", y);
}
